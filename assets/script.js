// use moment().format() to get the date 
$("#currentDay").text(moment().format("MMM Do, YYYY"));

// allow time blocks to change color based on current time

// save button adds text to local storage
var saveButton = $('#saveBtn');